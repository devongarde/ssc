ssc's json-ld parser is NOT written to support full json-ld processing,
rather to simply permit testing of schema. It is, in consequence, incomplete.
You will find the schema being tested in these directories.